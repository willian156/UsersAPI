apiVersion: apps/v1
kind: Deployment
metadata:
  name: users-api
  namespace: fcg
spec:
  replicas: 1
  selector:
    matchLabels:
      app: users-api
  template:
    metadata:
      labels:
        app: users-api
    spec:
      containers:
        - name: users-api
          image: fcg/users-api:cors-v1
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: users-api-config
            - secretRef:
                name: fcg-secrets
          env:
            - name: ConnectionStrings__Database
              valueFrom:
                secretKeyRef:
                  name: fcg-secrets
                  key: UsersDatabase
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
